syntax = "proto3";
package stream;

message Config {
  RateConfig record_rate = 1;
}

message RateConfig {
  // Minimum time between keyframes in nanoseconds
  double keyframe_frequency = 1;
  // Minimum time between mutations in nanoseconds
  double change_frequency = 2;
}
